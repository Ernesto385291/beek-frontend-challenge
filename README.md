# Beek Frontend Challenge

## Installation

Use the package manager npm to install the dependencies from the project.

```bash
npm install
```

## Usage

```bash
sudo npm run build && npm run start
```
